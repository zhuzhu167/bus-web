export const LOGIN = (state, data) => {
  state.is_login = data
}

export const SET_USERNAME = (state, data) => {
  state.userName = data
}

export const SET_ISREMIND = (state, data) => {
  state.is_remind = data
}

export const SET_CLOCKLIST = (state, data) => {
  state.clockList = data
}

export const SET_LOSTLIST = (state, data) => {
  state.lostList = data
}

export const SET_MYFEEDBACK = (state, data) => {
  state.myFeedbackList = data
}
